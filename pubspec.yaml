name: weekly_tab_pager
description: Customizable TabBar component with integrated TabView that displays weekdays and allows for infinite scrolling by week.

repository: https://github.com/azalessky/weekly_tab_pager
issue_tracker: https://github.com/azalessky/weekly_tab_pager/issues

version: 0.0.6
homepage: https://indentix.com
topics:
  - scrollable
  - tabs
  - weekdays
  - calendar
  - navigation

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0