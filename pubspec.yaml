name: weekly_tab_pager
description: A scrollable TabBar with attached TabView showing weekdays.
version: 0.0.1
homepage: http://indentix.com
topics:
  - scrollable
  - tabs
  - weekdays
  - calendar
  - navigation

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0