name: date_tab_pager
description: Date-driven TabBar and TabView with linked navigation and infinite scroll.

repository: https://github.com/azalessky/date_tab_pager
issue_tracker: https://github.com/azalessky/date_tab_pager/issues

version: 0.0.1
homepage: https://indentix.com
topics:
  - scrollable
  - tabs
  - weekdays
  - calendar
  - navigation

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0